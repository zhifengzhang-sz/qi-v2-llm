{
  "name": "qi-v2-llm",
  "version": "1.0.0",
  "description": "Qwen3 integration development environment",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && npm run setup:dependencies",
    "setup:dependencies": "echo 'Setting up development dependencies'",
    "docker:china": "chmod +x scripts/docker-china-mode.sh && ./scripts/docker-china-mode.sh",
    "docker:global": "chmod +x scripts/docker-global-mode.sh && ./scripts/docker-global-mode.sh",
    "github:china": "chmod +x scripts/github-china-mode.sh && ./scripts/github-china-mode.sh",
    "github:global": "chmod +x scripts/github-global-mode.sh && ./scripts/github-global-mode.sh",
    "network:china": "npm run docker:china && npm run github:china",
    "network:global": "npm run docker:global && npm run github:global",
    "build": "cd .devcontainer && (docker compose build || docker-compose build)",
    "build:base": "cd .devcontainer && (docker compose build base || docker-compose build base)",
    "start": "cd .devcontainer && (docker compose up -d || docker-compose up -d)",
    "stop": "cd .devcontainer && (docker compose down || docker-compose down)",
    "compose:install": "chmod +x scripts/install-compose.sh && ./scripts/install-compose.sh",
    "python": "cd .devcontainer && (docker compose run --rm python zsh || docker-compose run --rm python zsh)",
    "typescript": "cd .devcontainer && (docker compose run --rm typescript zsh || docker-compose run --rm typescript zsh)",
    "texlive": "cd .devcontainer && (docker compose run --rm texlive zsh || docker-compose run --rm texlive zsh)",
    "mcp": "cd .devcontainer && (docker compose run --rm mcp zsh || docker-compose run --rm mcp zsh)",
    "secrets:setup-deepseek": "chmod +x scripts/setup-deepseek.sh && ./scripts/setup-deepseek.sh",
    "secrets:setup-qwen3": "chmod +x scripts/setup-qwen3.sh && ./scripts/setup-qwen3.sh",
    "deepseek:test": "cd .devcontainer && (docker compose run --rm -v $(cd .. && pwd)/scripts:/scripts mcp python /scripts/test-deepseek-api.py || docker-compose run --rm -v $(cd .. && pwd)/scripts:/scripts mcp python /scripts/test-deepseek-api.py)",
    "qwen3:test": "cd .devcontainer && (docker compose run --rm -v $(cd .. && pwd)/scripts:/scripts mcp python /scripts/test-qwen3-api.py || docker-compose run --rm -v $(cd .. && pwd)/scripts:/scripts mcp python /scripts/test-qwen3-api.py)",
    "test:qwen3-235b": "python3 scripts/test-qwen3-235b-api.py --api-key $DASHSCOPE_API_KEY --api-base $DASHSCOPE_API_BASE",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "openai": "^3.2.1",
    "alibabacloud-dashscope-2023-05-15": "^1.0.0"
  },
  "devDependencies": {
    "eslint": "^8.28.0",
    "prettier": "^2.7.1",
    "jest": "^29.2.4",
    "supertest": "^4.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}